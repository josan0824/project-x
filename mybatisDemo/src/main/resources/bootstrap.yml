server:
  port: 8082
spring:
  application:
    name: mybatisDemo
  cloud:
    nacos:
      config:
        server-addr: ${NACOS_CONFIG_ADDR}
        file-extension: yml
      discovery:
        server-addr: ${NACOS_CONFIG_ADDR}
