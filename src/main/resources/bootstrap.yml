spring:
  application :
    name : yc-demo
  cloud:
    nacos:
      config:
        server-addr: ${NACOS_CONFIG_ADDR}
        file-extension: yml
      discovery:
        server-addr: ${NACOS_CONFIG_ADDR}