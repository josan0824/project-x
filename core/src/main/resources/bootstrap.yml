NACOS_CONFIG_ADDR: http://localhost:8848
spring:
  application :
    name : yc-core
  cloud:
    nacos:
      config:
        server-addr: ${NACOS_CONFIG_ADDR}
        file-extension: yaml
      discovery:
        server-addr: ${NACOS_CONFIG_ADDR}